version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: 'file:/app/dev.db'
    volumes:
      - ./dev.db:/app/dev.db  # Persist SQLite database
      - ./uploads:/app/uploads  # Example for uploads, optional
    restart: unless-stopped
